# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'rapleaf_api' do
  # developer('FIX', 'FIX@example.com')

  # self.rubyforge_name = 'rapleaf_apix' # if different than 'rapleaf_api'
end

# vim: syntax=ruby
